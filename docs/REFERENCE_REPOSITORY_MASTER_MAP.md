# Crypto reference repository master map

Status: nine pinned repositories are analyzed and applied through clean native
contracts. This is engineering provenance, not legal advice and not evidence of
profitability or Bitvavo live readiness.

The machine-readable authority is
`output/reference_integration/reference_master_map.json`, generated by
`reporting.reference_repository_master_map.build_reference_master_map`.

| Repository | Native responsibility | Native owner | Runtime coupling | Current gate |
|---|---|---|---|---|
| NautilusTrader | event-sourced execution state and reconciliation | `execution.canonical_state` | none | canonical replay tests |
| LEAN | InvestmentIntent -> PortfolioTarget -> Risk -> Execution | `portfolio.targets` | none | legacy direct callers remain debt |
| vectorbt | fast Stage-0 rejection | `research.research_factory` | isolated diagnostic only | exact native validation remains authoritative |
| PyBroker | chronological purged walk-forward | `research.research_factory` | isolated diagnostic only | final-test isolation required |
| Qlib | dataset/experiment/model provenance | `ml.registry` | isolated diagnostic only | SHADOW and prospective PIT data only |
| Freqtrade/FreqAI | lookahead/warmup/expiry controls | `ml.lifecycle` | isolated diagnostic only | leakage/staleness fail closed |
| FinRL | position-management RL state/action/reward and baselines | `rl.position_management` | isolated diagnostic only | training blocked pending data and dependencies |
| RD-Agent | hypothesis/experiment/feedback memory | `research.autonomous_rd` | isolated diagnostic only | no promotion authority |
| TradingAgents | bull/bear/risk structured evidence | `core.structured_market_intelligence` | isolated diagnostic only | SHADOW NO_TRADE hand-off only |

Every entry has a positive usage count, exact commit/tree/license provenance,
named upstream source evidence, native call sites and tests in the JSON artifact.
The reference clones remain clean and unchanged. None receives credentials,
Bitvavo clients, canonical-state writes, risk changes, or order authority.

## Canonical boundary

`reference concept -> native evidence/contract -> InvestmentIntent -> PortfolioTarget -> deterministic risk/authority -> execution -> Bitvavo`

AI/RL output cannot skip this chain. Current AI output is SHADOW and is converted
to a `NO_TRADE` InvestmentIntent. RL is not eligible to train until prospective
episode, regime, baseline, multi-seed, stress, point-in-time, cost and dependency
gates all pass.
